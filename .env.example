# Vite dev server
VITE_DEV_HOST=0.0.0.0
VITE_DEV_PORT=5173
VITE_DEV_PROXY_TARGET=http://localhost:5000

# Docker runtime (nginx template variables)
NGINX_PORT=80
API_UPSTREAM=http://localhost:5000

# API docs link in UI (must be proxied path)
VITE_API_DOCS_PATH=/api/
